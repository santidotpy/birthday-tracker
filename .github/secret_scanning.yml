# Rutas donde secret scanning no tiene que alertar.
#
# Lo más específico posible y con el motivo escrito: excluir una ruta también
# apaga la push protection ahí, así que cada línea de acá es una guarda menos.
# Revisar de vez en cuando y sacar lo que ya no aplique.

paths-ignore:
  # Fixtures de tests de autenticación. Las contraseñas de este archivo son
  # inventadas y viven en la base en memoria del test: 'la-que-no-es' y
  # 'otra-clave-larga-123' existen para comprobar que el login las rechaza.
  - 'src/auth/servidor.test.ts'
